# Resources

ME-LLM uses a frozen BERT-base backbone (`bert-base-uncased`, 768-dim, 12 layers) as its pretrained language model. The backbone weights are standard and are downloaded automatically by the training code from the HuggingFace Hub; no custom resource files are required in this release.
